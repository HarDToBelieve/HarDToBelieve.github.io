<def f='webkit/Source/JavaScriptCore/runtime/JSCell.h' l='127' type='JSC::JSCellLock &amp; JSC::JSCell::cellLock()'/>
<doc f='webkit/Source/JavaScriptCore/runtime/JSCell.h' l='120'>// Each cell has a built-in lock. Currently it&apos;s simply available for use if you need it. It&apos;s
    // a full-blown WTF::Lock. Note that this lock is currently used in JSArray and that lock&apos;s
    // ordering with the Structure lock is that the Structure lock must be acquired first.

    // We use this abstraction to make it easier to grep for places where we lock cells.
    // to lock a cell you can just do:
    // Locker locker { cell-&gt;cellLocker() };</doc>
<use f='webkit/Source/JavaScriptCore/dfg/DFGAbstractInterpreterInlines.h' l='2258' u='c' c='_ZN3JSC3DFG19AbstractInterpreter14executeEffectsEjPNS0_4NodeE'/>
<use f='webkit/Source/JavaScriptCore/runtime/WeakMapImpl.h' l='213' u='c' c='_ZN3JSC11WeakMapImpl14finishCreationERNS_2VME'/>
<def f='webkit/WebKitBuild/Debug/JavaScriptCore.framework/Versions/A/PrivateHeaders/JSCell.h' l='127' type='JSC::JSCellLock &amp; JSC::JSCell::cellLock()'/>
<doc f='webkit/WebKitBuild/Debug/JavaScriptCore.framework/Versions/A/PrivateHeaders/JSCell.h' l='120'>// Each cell has a built-in lock. Currently it&apos;s simply available for use if you need it. It&apos;s
    // a full-blown WTF::Lock. Note that this lock is currently used in JSArray and that lock&apos;s
    // ordering with the Structure lock is that the Structure lock must be acquired first.

    // We use this abstraction to make it easier to grep for places where we lock cells.
    // to lock a cell you can just do:
    // Locker locker { cell-&gt;cellLocker() };</doc>
