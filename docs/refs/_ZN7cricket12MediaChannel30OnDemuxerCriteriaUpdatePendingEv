<dec f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/base/media_channel.h' l='221' type='void cricket::MediaChannel::OnDemuxerCriteriaUpdatePending()'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/base/media_channel.h' l='212'>// Informs the media channel when the transport&apos;s demuxer criteria is updated.
  // * OnDemuxerCriteriaUpdatePending() happens on the same thread that the
  //   channel&apos;s streams are added and removed (worker thread).
  // * OnDemuxerCriteriaUpdateComplete() happens on the thread where the demuxer
  //   lives (network thread).
  // Because the demuxer is updated asynchronously, there is a window of time
  // where packets are arriving to the channel for streams that have already
  // been removed on the worker thread. It is important NOT to treat these as
  // new unsignalled ssrcs.</doc>
<ovr f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/engine/webrtc_video_engine.cc' l='1589' c='_ZN7cricket18WebRtcVideoChannel30OnDemuxerCriteriaUpdatePendingEv'/>
<ovr f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/engine/webrtc_voice_engine.cc' l='2060' c='_ZN7cricket23WebRtcVoiceMediaChannel30OnDemuxerCriteriaUpdatePendingEv'/>
<use f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/pc/channel.cc' l='489' u='c' c='_ZN7cricket11BaseChannel24RegisterRtpDemuxerSink_wEv'/>
<dec f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/media/base/media_channel.h' l='221' type='void cricket::MediaChannel::OnDemuxerCriteriaUpdatePending()'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/media/base/media_channel.h' l='212'>// Informs the media channel when the transport&apos;s demuxer criteria is updated.
  // * OnDemuxerCriteriaUpdatePending() happens on the same thread that the
  //   channel&apos;s streams are added and removed (worker thread).
  // * OnDemuxerCriteriaUpdateComplete() happens on the thread where the demuxer
  //   lives (network thread).
  // Because the demuxer is updated asynchronously, there is a window of time
  // where packets are arriving to the channel for streams that have already
  // been removed on the worker thread. It is important NOT to treat these as
  // new unsignalled ssrcs.</doc>
