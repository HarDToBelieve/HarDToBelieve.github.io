<dec f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/base/media_channel.h' l='881' type='void cricket::VideoMediaChannel::FillBitrateInfo(cricket::BandwidthEstimationInfo * bwe_info)'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/base/media_channel.h' l='873'>// This fills the &quot;bitrate parts&quot; (rtx, video bitrate) of the
  // BandwidthEstimationInfo, since that part that isn&apos;t possible to get
  // through webrtc::Call::GetStats, as they are statistics of the send
  // streams.
  // TODO(holmer): We should change this so that either BWE graphs doesn&apos;t
  // need access to bitrates of the streams, or change the (RTC)StatsCollector
  // so that it&apos;s getting the send stream stats separately by calling
  // GetStats(), and merges with BandwidthEstimationInfo by itself.</doc>
<ovr f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/media/engine/webrtc_video_engine.cc' l='1687' c='_ZN7cricket18WebRtcVideoChannel15FillBitrateInfoEPNS_23BandwidthEstimationInfoE'/>
<use f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/pc/channel.cc' l='983' u='c' c='_ZN7cricket12VideoChannel15FillBitrateInfoEPNS_23BandwidthEstimationInfoE'/>
<dec f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/media/base/media_channel.h' l='881' type='void cricket::VideoMediaChannel::FillBitrateInfo(cricket::BandwidthEstimationInfo * bwe_info)'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/media/base/media_channel.h' l='873'>// This fills the &quot;bitrate parts&quot; (rtx, video bitrate) of the
  // BandwidthEstimationInfo, since that part that isn&apos;t possible to get
  // through webrtc::Call::GetStats, as they are statistics of the send
  // streams.
  // TODO(holmer): We should change this so that either BWE graphs doesn&apos;t
  // need access to bitrates of the streams, or change the (RTC)StatsCollector
  // so that it&apos;s getting the send stream stats separately by calling
  // GetStats(), and merges with BandwidthEstimationInfo by itself.</doc>
