<dec f='webkit/Source/ThirdParty/libwebrtc/Source/third_party/boringssl/src/include/openssl/ssl.h' l='3903' type='int SSL_set_handshake_hints(SSL * ssl, const uint8_t * hints, size_t hints_len)'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/third_party/boringssl/src/include/openssl/ssl.h' l='3893'>// SSL_set_handshake_hints configures |ssl| to use |hints| as handshake hints.
// It returns one on success and zero on error. The handshake will then continue
// as before, but apply predicted values from |hints| where applicable.
//
// Hints may contain connection and session secrets, so they must not leak and
// must come from a source trusted to terminate the connection. However, they
// will not change |ssl|&apos;s configuration. The caller is responsible for
// serializing and applying options from the RPC server as needed. This ensures
// |ssl|&apos;s behavior is self-consistent and consistent with the caller&apos;s local
// decisions.</doc>
<def f='webkit/Source/ThirdParty/libwebrtc/Source/third_party/boringssl/src/ssl/handoff.cc' l='875' ll='953' type='int SSL_set_handshake_hints(SSL * ssl, const uint8_t * hints, size_t hints_len)'/>
