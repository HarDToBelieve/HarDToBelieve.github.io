<dec f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/checks.h' l='364'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/checks.h' l='359'>// The actual stream used isn&apos;t important. We reference |ignored| in the code
// but don&apos;t evaluate it; this is to avoid &quot;unused variable&quot; warnings (we do so
// in a particularly convoluted way with an extra ?: because that appears to be
// the simplest construct that keeps Visual Studio from complaining about
// condition being unused).</doc>
<dec f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/checks.h' l='364'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/checks.h' l='359'>// The actual stream used isn&apos;t important. We reference |ignored| in the code
// but don&apos;t evaluate it; this is to avoid &quot;unused variable&quot; warnings (we do so
// in a particularly convoluted way with an extra ?: because that appears to be
// the simplest construct that keeps Visual Studio from complaining about
// condition being unused).</doc>
