<dec f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/logging.h' l='477' type='static void rtc::LogMessage::AddLogToStream(rtc::LogSink * stream, rtc::LoggingSeverity min_sev)'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/logging.h' l='471'>// Stream: Any non-blocking stream interface.
  // Installs the |stream| to collect logs with severtiy |min_sev| or higher.
  // |stream| must live until deinstalled by RemoveLogToStream.
  // If |stream| is the first stream added to the system, we might miss some
  // early concurrent log statement happening from another thread happening near
  // this instant.</doc>
<def f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/logging.cc' l='290' ll='297' type='static void rtc::LogMessage::AddLogToStream(rtc::LogSink * stream, rtc::LoggingSeverity min_sev)'/>
<dec f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/logging.h' l='477' type='static void rtc::LogMessage::AddLogToStream(rtc::LogSink * stream, rtc::LoggingSeverity min_sev)'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/logging.h' l='471'>// Stream: Any non-blocking stream interface.
  // Installs the |stream| to collect logs with severtiy |min_sev| or higher.
  // |stream| must live until deinstalled by RemoveLogToStream.
  // If |stream| is the first stream added to the system, we might miss some
  // early concurrent log statement happening from another thread happening near
  // this instant.</doc>
