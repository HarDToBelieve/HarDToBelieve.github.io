<def f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/thread.h' l='489' type='bool rtc::Thread::RunningForTest()'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/rtc_base/thread.h' l='483'>// Expose private method IsRunning() for tests.
  //
  // DANGER: this is a terrible public API.  Most callers that might want to
  // call this likely do not have enough control/knowledge of the Thread in
  // question to guarantee that the returned value remains true for the duration
  // of whatever code is conditionally executing because of the return value!</doc>
<def f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/thread.h' l='489' type='bool rtc::Thread::RunningForTest()'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/rtc_base/thread.h' l='483'>// Expose private method IsRunning() for tests.
  //
  // DANGER: this is a terrible public API.  Most callers that might want to
  // call this likely do not have enough control/knowledge of the Thread in
  // question to guarantee that the returned value remains true for the duration
  // of whatever code is conditionally executing because of the return value!</doc>
