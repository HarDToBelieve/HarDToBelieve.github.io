<dec f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/port_allocator.h' l='362' type='bool cricket::PortAllocator::SetConfiguration(const cricket::ServerAddresses &amp; stun_servers, const std::vector&lt;RelayServerConfig&gt; &amp; turn_servers, int candidate_pool_size, bool prune_turn_ports, webrtc::TurnCustomizer * turn_customizer = nullptr, const absl::optional&lt;int&gt; &amp; stun_candidate_keepalive_interval = absl::nullopt)'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/port_allocator.h' l='349'>// Set STUN and TURN servers to be used in future sessions, and set
  // candidate pool size, as described in JSEP.
  //
  // If the servers are changing, and the candidate pool size is nonzero, and
  // FreezeCandidatePool hasn&apos;t been called, existing pooled sessions will be
  // destroyed and new ones created.
  //
  // If the servers are not changing but the candidate pool size is, and
  // FreezeCandidatePool hasn&apos;t been called, pooled sessions will be either
  // created or destroyed as necessary.
  //
  // Returns true if the configuration could successfully be changed.
  // Deprecated</doc>
<def f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/port_allocator.cc' l='120' ll='132' type='bool cricket::PortAllocator::SetConfiguration(const cricket::ServerAddresses &amp; stun_servers, const std::vector&lt;RelayServerConfig&gt; &amp; turn_servers, int candidate_pool_size, bool prune_turn_ports, webrtc::TurnCustomizer * turn_customizer = nullptr, const absl::optional&lt;int&gt; &amp; stun_candidate_keepalive_interval = absl::nullopt)'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/port_allocator.cc' l='119'>// Deprecated</doc>
<dec f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/p2p/base/port_allocator.h' l='362' type='bool cricket::PortAllocator::SetConfiguration(const cricket::ServerAddresses &amp; stun_servers, const std::vector&lt;RelayServerConfig&gt; &amp; turn_servers, int candidate_pool_size, bool prune_turn_ports, webrtc::TurnCustomizer * turn_customizer = nullptr, const absl::optional&lt;int&gt; &amp; stun_candidate_keepalive_interval = absl::nullopt)'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/p2p/base/port_allocator.h' l='349'>// Set STUN and TURN servers to be used in future sessions, and set
  // candidate pool size, as described in JSEP.
  //
  // If the servers are changing, and the candidate pool size is nonzero, and
  // FreezeCandidatePool hasn&apos;t been called, existing pooled sessions will be
  // destroyed and new ones created.
  //
  // If the servers are not changing but the candidate pool size is, and
  // FreezeCandidatePool hasn&apos;t been called, pooled sessions will be either
  // created or destroyed as necessary.
  //
  // Returns true if the configuration could successfully be changed.
  // Deprecated</doc>
