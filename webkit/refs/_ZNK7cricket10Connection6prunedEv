<def f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/connection.h' l='175' type='bool cricket::Connection::pruned() const'/>
<doc f='webkit/Source/ThirdParty/libwebrtc/Source/webrtc/p2p/base/connection.h' l='171'>// Called when a connection is determined to be no longer useful to us.  We
  // still keep it around in case the other side wants to use it.  But we can
  // safely stop pinging on it and we can allow it to time out if the other
  // side stops using it as well.</doc>
<def f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/p2p/base/connection.h' l='175' type='bool cricket::Connection::pruned() const'/>
<doc f='webkit/WebKitBuild/Debug/usr/local/include/webrtc/p2p/base/connection.h' l='171'>// Called when a connection is determined to be no longer useful to us.  We
  // still keep it around in case the other side wants to use it.  But we can
  // safely stop pinging on it and we can allow it to time out if the other
  // side stops using it as well.</doc>
